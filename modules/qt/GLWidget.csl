/*
	Component that provides an OpenGL widget.
	Also dispatches the widget events to its delegate interfaces.
*/
component GLWidget
{
	provides IObjectSource self; //< QObject reference
	receives IPainter painter;   //< Painter delegate
	receives IInputListener inputListener; //<< Input event delegate
};
